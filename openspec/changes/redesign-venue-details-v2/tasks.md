# Tasks: Redesign Venue Details to V2

- [ ] **Infrastructure & Models**
    - [ ] Update `Venue` model to include `certificates`, `features`, and `instagramUsername` if missing.
    - [ ] Update `AppColors` and `AppTheme` with the new design tokens (Nude color, specific shadows, glassmorphism styles).
- [ ] **Core Layout Redesign**
    - [ ] Modify `VenueDetailsScreen` to transition from 4 tabs to a more overview-centric layout.
    - [ ] Implement the new `NestedScrollView` header with updated `VenueHero`.
- [ ] **Component Development (V2)**
    - [ ] Create/Update `VenueHeroV2`: pagination, glass buttons, rounded transition.
    - [ ] Create `VenueIdentityV2`: Title, Location, category, and the floating logo card.
    - [ ] Create `VenueQuickActionsV2`: 4-column grid with updated icons and labels.
    - [ ] Create `ExpertsSection`: Horizontal scrollable list as shown in the design.
    - [ ] Create `WorkingHoursCardV2`: List format matching the design, including the "currently open" badge.
    - [ ] Create `MapPreviewV2`: Integrated map image with "See on Map" overlay.
    - [ ] Create `TrustBadgesGrid`: Certificates and awards section.
    - [ ] Create `FeaturesGrid`: Amenities chips.
    - [ ] Create `ReviewsPreview`: Minimalist review card as shown in the design.
- [ ] **Integration & Polish**
    - [ ] Update `AboutTab` to use all the new components in a single column.
    - [ ] Update `BookingBottomBar` to include the starting price and match the button style.
    - [ ] Ensure dark mode compatibility for all new components.
    - [ ] Verify 1:1 visual alignment with `mekan_detay_sayfası_2`.
- [ ] **Validation**
    - [ ] Run `openspec validate redesign-venue-details-v2 --strict`.
    - [ ] Verification on device/emulator.
